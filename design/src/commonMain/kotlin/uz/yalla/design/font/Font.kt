package uz.yalla.design.font

import org.jetbrains.compose.resources.FontResource

// Font resource names originate from yalla-design (tokens/fonts.json). The per-platform actuals
// are hand-finished afterwards; this file holds only the platform font handles.

internal expect val boldFont: FontResource
internal expect val mediumFont: FontResource
internal expect val normalFont: FontResource
